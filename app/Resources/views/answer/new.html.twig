{% extends 'base.html.twig' %}

{% block body %}
<main class="container">
    <div class="row">
        <div class="col s12 m8 offset-m2 l6 offset-l3">
            <h1>Answer creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" class="btn right"/>
            {{ form_end(form) }}

            <a href="{{ path('admin_default_index') }}" class="btn">Back to the list</a>
        </div>
    </div>
</main>
{% endblock %}
